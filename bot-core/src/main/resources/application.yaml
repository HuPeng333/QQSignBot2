spring:
  profiles:
    active: dev
  application:
    name: bot-core

simbot:
  component:
    mirai:
      deviceInfoFile: "./config/device.json"
  core:
    scan-package: priv.xds.listener
dubbo:
  consumer:
    check: false

